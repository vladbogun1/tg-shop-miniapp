/** Date/status helpers. The implementations live in @shop/shared so the admin cannot drift. */
export {
  dayLabel,
  formatDate,
  formatShortDateTime as formatDateTime,
  formatTime,
  ORDER_STATUS_COLOR,
  ORDER_STATUS_LABEL,
  ORDER_TIMELINE,
  shortOrderId,
} from "@shop/shared";
