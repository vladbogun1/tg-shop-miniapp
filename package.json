{
  "name": "tg-shop-v2",
  "private": true,
  "description": "Monorepo root: shared library + customer Mini App + admin panel",
  "workspaces": [
    "shared",
    "frontend",
    "frontend-admin"
  ],
  "scripts": {
    "build": "npm run build -w frontend && npm run build -w frontend-admin",
    "lint": "npm run lint -w frontend && npm run lint -w frontend-admin",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w frontend-admin"
  }
}
