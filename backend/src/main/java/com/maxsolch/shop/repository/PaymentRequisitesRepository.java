package com.maxsolch.shop.repository;

import com.maxsolch.shop.domain.PaymentRequisites;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaymentRequisitesRepository extends JpaRepository<PaymentRequisites, Integer> {
}
